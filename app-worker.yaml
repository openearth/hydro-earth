# front-end service and web app

runtime: python
env: flex
entrypoint: psqworker hydroearth.tasks.worker.models_queue_model1

runtime_config:
  python_version: 3

manual_scaling:
  instances: 1

resources:
  cpu: .5
  memory_gb: 0.9
  disk_size_gb: 10
